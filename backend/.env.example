# Banco de dados (obrigatório)
DATABASE_URL=postgresql://usuario:senha@localhost:5432/plataforma_imobiliaria

# Auth (obrigatório em produção)
JWT_SECRET=altere-em-producao-use-uma-string-longa-aleatoria

# MCP Server (para webhook/agente chamar tools)
MCP_SERVER_URL=http://localhost:8000

# Servidor
PORT=3000
NODE_ENV=development
